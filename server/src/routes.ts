import express, { Request, Response } from 'express'

const routes = express.Router()

routes.post('/startGame', async function startGame(request: Request, response: Response) {
    
})

export default routes
